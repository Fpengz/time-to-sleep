# Archived Python Backend

This directory contains the original Python prototype and test suite for Time-to-Sleep.

As of August 2026, Time-to-Sleep has transitioned entirely to a compiled native Rust backend (`src/`) for single-binary packaging, sub-microsecond formatting latency, and minimal resource utilization.

The files here are retained purely for historical reference and benchmarking comparisons.
